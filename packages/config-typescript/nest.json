{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "SportBrainHQ NestJS",
  "docs": [
    "experimentalDecorators and emitDecoratorMetadata are required: Nest's dependency injection reads parameter types at runtime from emitted metadata.",
    "strictPropertyInitialization is off because Nest injects into declared-but-unassigned constructor properties."
  ],
  "extends": "./base.json",
  "compilerOptions": {
    "module": "CommonJS",
    "moduleResolution": "node",
    "types": ["node"],
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "strictPropertyInitialization": false
  }
}
