/**
 * Tailwind v4 runs as a PostCSS plugin and reads its configuration from the
 * `@theme` block in app/globals.css. There is no tailwind.config.js by design.
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
